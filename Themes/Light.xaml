<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="RadioButton">
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Background">
            <Setter.Value>
                <SolidColorBrush Color="#F0F0F0" Opacity="0.9"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Foreground" Value="PaleGoldenrod"/>
    </Style>

    <Style x:Key="CalcButtonStyle">
        <Setter Property="Control.Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0.5,1" EndPoint="0.5,0">
                    <GradientStop Color="DarkBlue" Offset="0"/>
                    <GradientStop Color="Lavender" Offset="1"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="Control.Foreground" Value="White"/>
        <Setter Property="Control.FontFamily" Value="Verdana"/>
        <Setter Property="Control.FontWeight" Value="Bold"/>
        <Setter Property="Control.Margin" Value="0,10,0,10"/>
    </Style>

    <SolidColorBrush x:Key="WindowBackground" Color="WhiteSmoke" />

    <SolidColorBrush x:Key="GroupBoxBackground" Color="#FAFAFA" />
    
</ResourceDictionary>